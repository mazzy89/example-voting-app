{{- if .Values.result.ingress.enabled -}}
{{- $fullName := include "example-voting-app.fullname" . -}}
{{- $svcPort := .Values.result.service.port -}}
{{- if and .Values.result.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.result.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.result.ingress.annotations "kubernetes.io/ingress.class" .Values.result.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-result
  labels:
    app: {{ template "example-voting-app.fullname" . }}-result
    {{- include "example-voting-app.labels" . | nindent 4 }}
  {{- with .Values.result.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.result.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.result.ingress.className }}
  {{- end }}
  {{- if .Values.result.ingress.tls }}
  tls:
    {{- range .Values.result.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.result.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-result
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-result
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
